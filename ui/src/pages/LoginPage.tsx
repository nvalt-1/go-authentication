import React from "react";

export default function LoginPage(): React.ReactElement {
    return (<>
        <header>Login</header>
        <main>
            <form>
                <input type='text' />
            </form>
        </main>
    </>);
}